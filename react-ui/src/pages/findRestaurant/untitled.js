daily average
avergage for detail firm for each day compared to total average for all the other firms, whether by all or by filter.




find total average for each day first for res.data, pass down date
totaalAvg = [{avgcheckins: 23, date: 12/30/2017}]

compare all the values for each same date
show the two values



indivRestuarnat = [{checkins: 40, date: 12/30/2017}]
date = inidvREstaurant.date
findtingTotalAvgPerDay(date)
findingTotalAvgPerDay = function(date) {
	var restaurants [
	{
	aslkds:

	}
	{}
	]

	Array.filter(date => function())

	db.find()
	restaurants.checkins with date 
		find mean
}

restaurants.forEach(item => {
	var index = item.checkins.filter(x => x.date === item.date)

	console.log(index)
})